<Window x:Name="wifi_checkbox" x:Class="Communication_Manager.Window1"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Communication_Manager"
        mc:Ignorable="d"
        Title="Window1" Height="422" Width="712">
    <Grid Height="381" VerticalAlignment="Top" HorizontalAlignment="Left" Width="649">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="263*"/>
            <ColumnDefinition Width="262*"/>
        </Grid.ColumnDefinitions>
        <CheckBox x:Name="checkBox" Content="Wifi" HorizontalAlignment="Left" Margin="59,30,0,0" VerticalAlignment="Top" Checked="checkBox_Checked"/>
        <CheckBox x:Name="checkBox1_Copy" Content="Mobile" Margin="59,181,85,123" Checked="checkBox1_Copy_Checked"/>
        <ListView x:Name="wifi_listView" HorizontalAlignment="Left" Height="100" Margin="10,56,0,0" VerticalAlignment="Top" Width="311" Grid.ColumnSpan="2" SelectionChanged="wifi_listView_SelectionChanged" BorderThickness="2">
            <ListView.Effect>
                <DropShadowEffect/>
            </ListView.Effect>
            <ListView.View>
                <GridView>
                    <GridViewColumn/>
                </GridView>
            </ListView.View>
        </ListView>
        <ListView x:Name="mobile_listView" HorizontalAlignment="Left" Height="100" Margin="10,209,0,0" VerticalAlignment="Top" Width="311" Grid.ColumnSpan="2" SelectionChanged="listView1_SelectionChanged" BorderThickness="2">
            <ListView.Effect>
                <DropShadowEffect/>
            </ListView.Effect>
            <ListView.View>
                <GridView>
                    <GridViewColumn/>
                </GridView>
            </ListView.View>
        </ListView>
        <Button x:Name="wifi_connect_button" HorizontalAlignment="Left" Margin="37,56,0,0" VerticalAlignment="Top" Width="120" Height="25" Content="wifi connect" Grid.Column="1" Click="wifi_connect_button_Click"/>
        <Button x:Name="mobile_connect_button" Content="mobile connect" HorizontalAlignment="Left" Margin="36,209,0,0" VerticalAlignment="Top" Width="120" Grid.Column="1" Click="mobile_connect_button_Click" RenderTransformOrigin="0.5,0.5" Height="27">
            <Button.RenderTransform>
                <TransformGroup>
                    <ScaleTransform/>
                    <SkewTransform AngleX="-2.603"/>
                    <RotateTransform/>
                    <TranslateTransform X="-0.455"/>
                </TransformGroup>
            </Button.RenderTransform>
        </Button>
        <Label x:Name="wifi_password_label" Content="Password" HorizontalAlignment="Left" Margin="37,102,0,0" VerticalAlignment="Top" Width="204" Grid.Column="1" AutomationProperties.Name="wifi_password_label"/>
        <Label x:Name="mobile_password_label" Content="Password" HorizontalAlignment="Left" Margin="37,237,0,0" VerticalAlignment="Top" Width="226" Grid.Column="1"/>
        <PasswordBox x:Name="wifi_passwordBox" Grid.Column="1" HorizontalAlignment="Left" Margin="37,133,0,0" VerticalAlignment="Top" Width="164" Height="23"/>
        <PasswordBox x:Name="passwordBox1" HorizontalAlignment="Left" VerticalAlignment="Top"/>
        <PasswordBox x:Name="mobile_passwordBox" Grid.Column="1" HorizontalAlignment="Left" Margin="37,268,0,0" VerticalAlignment="Top" Width="164" Height="27"/>

    </Grid>
</Window>
